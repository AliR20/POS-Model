import 'package:flutter/material.dart';

class CashiersScreen extends StatelessWidget {
  const CashiersScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: const Text('Cashiers'),
      ),
    );
  }
}